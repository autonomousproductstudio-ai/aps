"""Brand tool namespace (Launch Studio Phase 1).

Four deterministic, stdlib-only tools the registry auto-discovers under namespace
"brand": logo SVG, brand sheet SVG, brand identity, launch campaign. No image model, no
network — they add ~0 wall-clock to a run.
"""
